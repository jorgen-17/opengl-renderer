 {
    "version": "0.2.0",
    "configurations": [
       {
            "type": "codelldb",
            "request": "launch",
            "preLaunchTask": "Build Debug",
            "name": "Run Debug Linux",
            "args": [],
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/main"
        }
    ]
}
